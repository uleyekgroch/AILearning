# 可行性评估：用 AILearning 系统学习 wiki_zh 百科语料

## 一、语料概况

| 指标 | 数值 |
|------|------|
| 总大小 | **1.27 GB** |
| 目录数 | 13 个 (AA~AM) |
| 文件数 | **1374** 个 (每目录 100 或 74 个) |
| 格式 | JSON Lines，每行一条 `{"id","url","title","text"}` |
| 平均单条 | ~1KB 标题 + ~10KB 正文（长文可达 50KB+） |
| 领域 | 全学科（数学、哲学、文学、历史、物理、化学、地理…） |
| 估算条目数 | **约 5~10 万条** |

## 二、我们的系统真正能做什么

### 2.1 能做的事（真实能力）

| 模块 | 能力 | 百科场景 | 真实效果 |
|------|------|---------|---------|
| **TextLearner** | 提取关键词/共现 | 从百科文本提取概念 | ✅ 可运行，提取词频和共现 |
| **StatisticalLearner** | 概念涌现 | 发现高频共现概念簇 | ✅ 可运行，但粒度粗糙 |
| **KnowledgeGraph** | 三元组存储 | title→category→relation | ✅ 可存储，但需预处理 |
| **ActiveExperimenter** | 假设生成 | 从观察生成"金属都导电"类假设 | ✅ 框架完整，但假设质量有限 |
| **MetaLearner** | 策略追踪 | 追踪哪种读法更有效 | ✅ 框架完整，但只有策略壳 |
| **EmotionEngine** | 情感调制 | 处理"惊喜"发现时增强记忆 | ✅ 机制完整 |
| **AnalogicalTransfer** | 类比迁移 | 物理知识迁移到工程 | ✅ 属性匹配可运行 |
| **IntegratedLearner** | 全流水线 | 端到端学习循环 | ✅ 可跑通 |

### 2.2 做不到的事（真实差距）

#### ❌ 差距 1：没有语义理解

```
输入: "数学是利用符号语言研究数量、结构、变化以及空间等概念的一门学科"

人类: 理解"数学"是抽象学科，"符号语言"是工具，"数量/结构/变化/空间"是研究对象
我们的系统: 把这句话当字符串，提取词频 {"数学":1, "符号语言":1, "研究":1, ...}
```

**根因**: 没有语言模型。TextLearner 做的是**词频统计**，不是语义理解。
人类大脑有 860 亿神经元形成的语言理解网络，我们只有 `std::map<std::string, int>`。

#### ❌ 差距 2：没有知识表示

```
输入: "高斯称数学为科学的皇后"

人类: 建立 [高斯] --称誉--> [数学] --比喻--> [科学皇后]
      理解这是比喻，不是字面意思
我们的系统: 存为字符串 "高斯称数学为科学的皇后"
           无法区分比喻和事实
```

**根因**: KnowledgeGraph 需要**预结构化**的三元组输入。我们的系统没有 NLP 能力从自然语言提取 `(主体, 关系, 客体)` 三元组。

#### ❌ 差距 3：没有推理能力

```
已知: "数学研究数量和结构"
已知: "代数研究数学结构"

人类推理: 代数 ⊂ 数学（代数是数学的子领域）
我们的系统: 无法推理出这个层次关系
```

**根因**: 没有逻辑推理引擎。我们的 reasoning 模块是**预测编码**（误差驱动），不是逻辑推理。

#### ❌ 差距 4：没有增量理解

```
人类读百科:
  第1篇"数学" → 建立数学的基本概念框架
  第2篇"哲学" → 发现"逻辑学"是数学和哲学的交叉领域
  第3篇"代数" → 把代数挂到数学框架下，发现与哲学的联系
  ...递进理解

我们的系统:
  第1篇 → 统计词频 {数学:50, 符号:8, ...}
  第2篇 → 统计词频 {哲学:40, 理性:12, ...}
  第3篇 → 统计词频 {代数:30, 结构:10, ...}
  之间没有真正的"理解递进"
```

#### ❌ 差距 5：没有主动学习目标

```
人类: 读到"费马大定理" → 产生好奇 → 主动搜索"椭圆曲线" → 发现新世界
我们: 只能被动接收文本，"好奇"是数值变量，不驱动真正的知识获取
```

## 三、如果强行运行，会发生什么

写一个简单的驱动程序，把 wiki_zh 的文本灌入 `Learner::learn_from_text()`：

```cpp
Learner learner(config);
// 读入 wiki_zh/AA/wiki_00 的每一行 JSON
for (auto& line : lines) {
    auto doc = parse_json(line);
    learner.learn_from_text(doc["text"], doc["title"]);
}
```

**预期结果**:
- ✅ 能运行，不会崩溃
- ✅ KnowledgeGraph 会积累节点和边
- ✅ StatisticalLearner 会积累共现统计
- ✅ 情感引擎会记录"学习进度"
- ❌ 但学到的都是**表层统计特征**，不是语义理解
- ❌ 读完 1.27GB 后，你问它"数学是什么"，它答不了
- ❌ 类比迁移、假设生成等高级模块无法产出有意义的结果

**类比**: 这就像让一个只懂拼音的婴儿朗读百科全书——声带在动，但没有理解。

## 四、与真实人类学习的差距量化

| 能力维度 | 人类 | 我们的系统 | 差距 |
|----------|------|-----------|------|
| **语言理解** | 深度语义、比喻、讽刺 | 词频统计 | ★★★★★ |
| **知识表示** | 本体、层次、因果、时序 | 扁平图结构 | ★★★★☆ |
| **推理** | 演绎、归纳、类比、因果 | 仅类比（属性匹配） | ★★★★★ |
| **记忆** | 工作记忆+长期记忆+情景记忆 | 有类似结构，但内容浅 | ★★★☆☆ |
| **元认知** | 真正的"知道自己不知道" | 策略推荐壳 | ★★★★☆ |
| **主动学习** | 内驱好奇+目标驱动+社会学习 | 有框架，但无真实目标 | ★★★★☆ |
| **情感调制** | 真正的兴奋/困惑/顿悟 | 数值模拟 | ★★★☆☆ |
| **跨域迁移** | 结构映射+深层关系 | 属性字符串匹配 | ★★★★★ |

> ★ = 基础框架存在但无实质能力；★越多差距越大

## 五、要让它"真正学习"百科，需要补什么

### 第一优先级：语言理解层

```cpp
// 需要接入预训练语言模型（如 BERT/GPT embedding）
struct SemanticEncoder {
    // "数学是研究数量的学科" → [0.12, -0.34, 0.56, ...] (768维向量)
    auto encode(const std::string& text) -> std::vector<float>;
    
    // 向量相似度 = 语义相似度
    auto similarity(const std::vector<float>& a, 
                    const std::vector<float>& b) -> float;
};
```

没有这一层，所有高级模块（类比/假设/顿悟）都是空中楼阁。

### 第二优先级：知识提取层

```cpp
// 需要信息抽取 (IE) 能力
struct KnowledgeExtractor {
    // "高斯称数学为科学的皇后" 
    // → (高斯, 称誉/比喻, 数学), (数学, 被比喻为, 科学皇后)
    auto extract_triples(const std::string& text) 
        -> std::vector<Triple>;
};
```

### 第三优先级：推理引擎

```cpp
// 需要逻辑推理（不只是统计）
struct ReasoningEngine {
    // 已知: 代数 ⊂ 数学, 数学是形式科学
    // 推理: 代数是形式科学的子领域
    auto entail(const Triple& a, const Triple& b) 
        -> std::optional<Triple>;
};
```

## 六、结论

| 问题 | 回答 |
|------|------|
| 能运行吗？ | ✅ 能。系统不会崩溃 |
| 能学到东西吗？ | ⚠️ 能学到**表层统计**，但不是语义理解 |
| 类人类学习？ | ❌ 差距巨大。缺少三个核心：语言理解、知识表示、推理 |
| 值得做吗？ | ✅ **值得**。作为框架验证，证明架构可端到端运行 |

**一句话总结**: 我们建了一个完美的**学习框架骨架**，但缺少让骨架活起来的**语义灵魂**。要真正学习百科，需要接入预训练语言模型的 embedding 能力——这正是下一步最有价值的方向。
