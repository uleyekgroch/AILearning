export const meta = {
  name: 'phase-9-extensions',
  description: 'Phase 9: Advanced Extensions - Multi-Agent Society, Language Interface (Tongyi/Qwen), Continuous Online Learning',
  phases: [
    { title: 'Core Classes', detail: 'Society + LLM Provider + Continuous Loop in parallel' },
    { title: 'REST Integration', detail: 'Society/Chat/Runtime endpoints in server.cpp' },
    { title: 'Web Console', detail: 'Update web UI for Phase 9 features' },
    { title: 'Verify', detail: 'Build + smoke test + chat test' },
  ],
}

// Phase 1: Core C++ classes — fully independent, no file conflicts
phase('Core Classes')

const society = await agent(
  'Active task: .trellis/tasks/06-02-ailearning-phase-7-9\n\n## Phase 9.1: Multi-Agent Society — Core C++ Classes\n\nCreate the Society class that manages multiple Learner agents as independent processes communicating via REST.\n\nFiles to CREATE:\n1. ai-learning-cpp/include/ai_learning/society/society.hpp — Society class header\n2. ai-learning-cpp/include/ai_learning/society/agent_handle.hpp — Agent wrapper (REST client to a Learner server)\n3. ai-learning-cpp/src/society/society.cpp — Society implementation\n4. ai-learning-cpp/src/society/agent_handle.cpp — Agent REST client\n\nArchitecture:\n```\nSociety (port 8090)\n  manages N AgentHandle instances\n  each AgentHandle wraps a REST connection to a Learner server (ports 8080, 8081, ...)\n  Society spawns Learner processes or connects to existing ones\n```\n\nREAD first:\n- ai-learning-cpp/include/ai_learning/core/learner.hpp — understand Learner API surface\n- ai-learning-cpp/include/ai_learning/learning/social_learning.hpp — BehaviorObservation, SocialLearningReport types\n- ai-learning-cpp/src/server/server.hpp — current server interface\n- ai-learning-cpp/src/server/server_config.hpp — ServerConfig\n\nAgentHandle class:\n- Constructor: AgentHandle(agent_id, host, port)\n- REST client methods using a minimal asio HTTP client:\n  - learn(text) → json (via POST /api/learn/text)\n  - reason(question) → json (via POST /api/reason)\n  - observe_behavior(observation_json) → json (via POST /api/observe-behavior)\n  - get_stats() → json (via GET /api/stats)\n  - health() → bool (via GET /api/health)\n- Minimal HTTP client using asio (already a dependency):\n  - Simple synchronous GET/POST with JSON body\n  - Parse JSON response from body\n  - Handle connection errors gracefully\n\nSociety class:\n- create_agent(port) → string agent_id: spawns new ai_learning_server process on given port\n- remove_agent(agent_id): terminate agent process\n- list_agents() → vector of AgentInfo\n- trigger_observation(observer_id, model_id, domain): make observer_agent call observe_behavior on model_agent\n- broadcast_knowledge(from_id, domain): share knowledge from one agent to all others\n- social_metrics() → SocietyMetrics struct\n\nSocietyMetrics struct: total_agents, active_agents, avg_knowledge_per_agent, knowledge_diversity, cultural_transmission_count, collective_learning_speed\n\nSocietyConfig struct: base_port(8080), max_agents(10), spawn_timeout_ms(5000)\nAgentInfo struct: agent_id, port, pid, status(active/stopped), knowledge_count\n\nDesign constraints:\n- Each agent runs as a SEPARATE PROCESS (subprocess)\n- Communication ONLY via REST HTTP (no shared memory)\n- On Windows: use CreateProcess to spawn agents; on Linux: fork+exec\n- Keep total code under 600 lines across all 4 files\n- Do NOT modify any existing files\n- Use nlohmann/json for JSON parsing\n- Use asio for HTTP client (already available via FetchContent)\n- #define ASIO_STANDALONE before including asio',
  { label: 'society-core', phase: 'Core Classes' }
)

const llmProvider = await agent(
  'Active task: .trellis/tasks/06-02-ailearning-phase-7-9\n\n## Phase 9.2: Language Interface — LLM Provider (Tongyi/Qwen) + Dialog Manager\n\nCreate the language interface using Alibaba Tongyi Qwen API (OpenAI-compatible mode).\n\nAPI VERIFIED WORKING:\n- Endpoint: https://dashscope.aliyuncs.com/compatible-mode/v1/chat/completions\n- Model: qwen-plus-latest\n- Auth: Bearer sk-b68b1187aba542c3b2fec09cdc02634c\n- Request: {\"model\":\"qwen-plus-latest\",\"messages\":[{\"role\":\"user\",\"content\":\"hello\"}]}\n- Response: {\"choices\":[{\"message\":{\"content\":\"...\",\"role\":\"assistant\"}}],\"usage\":{...}}\n\nFiles to CREATE:\n1. ai-learning-cpp/include/ai_learning/language/llm_provider.hpp — ILLMProvider interface + implementations\n2. ai-learning-cpp/include/ai_learning/language/dialog_manager.hpp — multi-turn dialog manager\n3. ai-learning-cpp/src/language/llm_provider.cpp — provider implementations\n4. ai-learning-cpp/src/language/dialog_manager.cpp — dialog manager implementation\n\nREAD first:\n- ai-learning-cpp/include/ai_learning/language/grounding.hpp — existing GroundingModule\n- ai-learning-cpp/include/ai_learning/language/development.hpp — DevelopmentTracker\n- ai-learning-cpp/include/ai_learning/core/learner.hpp — Learner API\n\n### ILLMProvider interface:\n```cpp\nclass ILLMProvider {\npublic:\n    virtual ~ILLMProvider() = default;\n    virtual auto complete(const std::string& prompt, const std::string& system_prompt = \"\") -> std::string = 0;\n};\n```\n\n### Implementations:\n\n1. StubLLMProvider — returns canned responses for testing (no API key needed)\n   - Pattern matching: if prompt contains \"explain\" return explanation-like text\n   - Default: \"I understand. Let me process that.\"\n   - This is the FALLBACK when no API key is configured\n\n2. OpenAICompatibleProvider — calls OpenAI-compatible APIs (Tongyi/Qwen primarily)\n   - Constructor: OpenAICompatibleProvider(base_url, api_key, model)\n   - Default config (hardcoded defaults in constructor):\n     - base_url = \"dashscope.aliyuncs.com\"\n     - api_key = env var DASHSCOPE_API_KEY, fallback \"sk-b68b1187aba542c3b2fec09cdc02634c\"\n     - model = \"qwen-plus-latest\"\n   - Uses asio for HTTPS POST to base_url + \"/compatible-mode/v1/chat/completions\"\n   - Headers: Authorization: Bearer <api_key>, Content-Type: application/json\n   - Request body: {\"model\": model, \"messages\": [{\"role\":\"system\",\"content\": system_prompt}, {\"role\":\"user\",\"content\": prompt}]}\n   - Response parsing: json[\"choices\"][0][\"message\"][\"content\"]\n   - Works with ANY OpenAI-compatible API (Tongyi, OpenAI, DeepSeek, etc.)\n\n   IMPORTANT: HTTPS implementation\n   - Since standalone asio does NOT have SSL/TLS built-in, and OpenSSL is not a dependency of this project:\n   - Use a SIMPLE APPROACH: spawn curl as a subprocess for the HTTPS request\n   - This avoids adding OpenSSL as a dependency\n   - On Windows: use _popen() to run curl command and capture output\n   - curl command: curl -s -X POST <url> -H \"Authorization: Bearer <key>\" -H \"Content-Type: application/json\" -d <body>\n   - Parse the JSON output with nlohmann/json\n   - This is pragmatic and works on any platform with curl available\n\n### DialogManager:\n```cpp\nclass DialogManager {\npublic:\n    explicit DialogManager(ILLMProvider& llm, core::Learner& learner);\n    auto chat(const std::string& user_message, const std::string& session_id = \"default\") -> DialogResponse;\n    auto get_history(const std::string& session_id, int last_n = 20) const -> std::vector<DialogTurn>;\n    void clear_session(const std::string& session_id);\n    auto list_sessions() const -> std::vector<std::string>;\nprivate:\n    auto build_system_prompt_() const -> std::string;\n    auto detect_intent_(const std::string& user_message) -> std::string;\n};\n```\n\nDialogResponse: assistant_message, knowledge_learned (vector<string>), learner_action, intent\nDialogTurn: role (user/assistant), content, timestamp\n\nIntent detection (keyword matching):\n- Chinese keywords: \"教\"/\"学习\"/\"是\"/\"关于\" → teach → learn_from_text()\n- \"为什么\"/\"怎么\"/\"如何\" → ask → reason()\n- \"试试\"/\"实验\" → explore → design_experiment()\n- \"思考\"/\"反思\" → reflect → meta_reflect()\n- Default: general → learn_from_text() if looks like factual content, else reason()\n\nSystem prompt (in Chinese):\n- 你是一个AI学习系统助手。当前发展阶段：<stage>。已掌握< N>个知识节点。情感状态：<emotion>。你可以帮助用户教系统新知识，或回答关于系统状态的问题。\n\nDesign constraints:\n- StubLLMProvider works WITHOUT any API key or network\n- DialogManager owns session state (map of session_id to vector of DialogTurn)\n- Max 20 turns per session, max 10 sessions\n- Total code under 600 lines across all 4 files\n- Do NOT modify any existing files\n- All user-facing text in Chinese\n- #define ASIO_STANDALONE before including asio (if needed)',
  { label: 'llm-dialog', phase: 'Core Classes' }
)

const continuousLoop = await agent(
  'Active task: .trellis/tasks/06-02-ailearning-phase-7-9\n\n## Phase 9.3: Continuous Online Learning — Loop Engine + Checkpoint Manager\n\nCreate the continuous learning loop and checkpoint/recovery infrastructure.\n\nFiles to CREATE:\n1. ai-learning-cpp/include/ai_learning/learning/continuous_loop.hpp — ContinuousLearningLoop + CheckpointManager\n2. ai-learning-cpp/src/learning/continuous_loop.cpp — implementation\n\nREAD first:\n- ai-learning-cpp/include/ai_learning/learning/continual_learner.hpp — ContinualLearner (EWC, replay)\n- ai-learning-cpp/include/ai_learning/learning/consolidation.hpp — SleepConsolidation\n- ai-learning-cpp/include/ai_learning/core/learner.hpp — Learner API\n\n### ContinuousLearningLoop class:\n```cpp\nclass ContinuousLearningLoop {\npublic:\n    explicit ContinuousLearningLoop(core::Learner& learner);\n    void start(const ContinuousLoopConfig& config = {});\n    void stop();\n    [[nodiscard]] bool is_running() const;\n    [[nodiscard]] LoopStatus status() const;\n    void feed_data(const std::string& data, const std::string& source);\n    void checkpoint_now();\n    bool recover(const std::string& checkpoint_dir);\nprivate:\n    auto loop_iteration_() -> LoopStepResult;\n    bool should_consolidate_() const;\n    bool should_checkpoint_() const;\n    auto ingest_pending_data_() -> int;\n    void run_loop_(ContinuousLoopConfig config);\n};\n```\n\nContinuousLoopConfig: checkpoint_interval_seconds(300), consolidation_interval_seconds(60), max_iterations(0=unlimited), data_buffer_size(100), checkpoint_dir(\"./checkpoints\"), auto_recover(true)\n\nLoopStatus: running(bool), iterations(int), data_processed(int), last_checkpoint_time(string), last_consolidation_time(string), knowledge_retention(double), uptime_seconds(double), pending_data_count(int)\n\nLoopStepResult: learned(int), consolidated(bool), checkpointed(bool), retention(double), errors(vector<string>)\n\n### CheckpointManager class:\n```cpp\nclass CheckpointManager {\npublic:\n    explicit CheckpointManager(const std::string& checkpoint_dir);\n    auto save(const core::Learner& learner, const std::string& tag = \"\") -> std::string;\n    auto load_latest(core::Learner& learner) -> bool;\n    auto load(core::Learner& learner, const std::string& checkpoint_id) -> bool;\n    auto list_checkpoints() const -> std::vector<CheckpointInfo>;\n    void cleanup(int keep_last = 5);\n    auto latest() const -> std::optional<CheckpointInfo>;\n};\n```\n\nCheckpointInfo: id(string), tag(string), timestamp(string), knowledge_count(int), file_size_bytes(int64_t)\n\nCheckpoint strategy:\n- Call learner.save(checkpoint_path) for Learner state\n- Create metadata JSON alongside: {id, timestamp, tag, knowledge_count, save_file}\n- Keep last 5 checkpoints, delete older ones\n- Recovery: find latest metadata JSON → load learner from referenced save file\n\nLoop iteration:\n1. Check pending data buffer → learn_from_text() for each\n2. Check consolidation timer → if due, consolidate()\n3. Check forgetting → if retention low, continual_learner().replay()\n4. Check checkpoint timer → if due, checkpoint\n5. Update status\n6. Sleep loop_interval (default 1s)\n\nDesign constraints:\n- Use std::filesystem for file operations\n- Background thread: std::thread + std::atomic\n- Graceful shutdown: stop() waits for current iteration\n- Total code under 500 lines\n- Do NOT modify any existing files',
  { label: 'continuous-loop', phase: 'Core Classes' }
)

log('Society: ' + (society ? 'done' : 'failed'))
log('LLM/Dialog: ' + (llmProvider ? 'done' : 'failed'))
log('Continuous Loop: ' + (continuousLoop ? 'done' : 'failed'))

// Phase 2: REST integration — single agent to avoid file conflicts on server.cpp
phase('REST Integration')

const restIntegration = await agent(
  'Active task: .trellis/tasks/06-02-ailearning-phase-7-9\n\n## Phase 9 REST Integration — Server Routes + CMakeLists\n\nAdd REST endpoints for Phase 9 features into the existing server.\n\nREAD FIRST (all required):\n- ai-learning-cpp/src/server/server.hpp — LearningServer class\n- ai-learning-cpp/src/server/server.cpp — existing route registration pattern\n- ai-learning-cpp/CMakeLists.txt — build config\n- ai-learning-cpp/include/ai_learning/society/society.hpp — Society class API\n- ai-learning-cpp/include/ai_learning/language/dialog_manager.hpp — DialogManager API\n- ai-learning-cpp/include/ai_learning/language/llm_provider.hpp — ILLMProvider\n- ai-learning-cpp/include/ai_learning/learning/continuous_loop.hpp — ContinuousLearningLoop\n\n### Tasks:\n\n#### 1. Update server.hpp\nAdd new private methods:\n- register_society_routes_(crow::SimpleApp& app)\n- register_chat_routes_(crow::SimpleApp& app)\n- register_runtime_routes_(crow::SimpleApp& app)\n\nAdd new members:\n- std::unique_ptr<society::Society> society_ (lazy init)\n- std::unique_ptr<language::DialogManager> dialog_ (lazy init)\n- std::unique_ptr<learning::ContinuousLearningLoop> continuous_loop_ (lazy init)\n- std::unique_ptr<language::ILLMProvider> llm_provider_ (lazy init)\n\nAdd includes for the new headers.\n\n#### 2. Add routes in server.cpp\nCall the 3 new register_* methods in register_routes_().\n\nSociety routes (register_society_routes_):\n- POST /society/create → create_agent(port)\n- DELETE /society/agents/{id} → remove_agent(id)\n- GET /society/agents → list_agents()\n- POST /society/observe → trigger_observation(...)\n- POST /society/broadcast → broadcast_knowledge(...)\n- GET /society/metrics → social_metrics()\n\nChat routes (register_chat_routes_):\n- POST /api/chat → dialog_->chat(message, session_id)\n  Body: {\"message\": \"...\", \"session_id\": \"default\"}\n- GET /api/chat/history?session=default → dialog history\n- DELETE /api/chat/session/{id} → clear session\n\nRuntime routes (register_runtime_routes_):\n- GET /api/runtime/status → status()\n- POST /api/runtime/start → start(config)\n- POST /api/runtime/stop → stop()\n- POST /api/runtime/checkpoint → checkpoint_now()\n- POST /api/runtime/feed → feed_data(data, source)\n- POST /api/runtime/recover → recover(dir)\n\n#### 3. Update CMakeLists.txt\nAdd to ai_learning library sources (GLOB_RECURSE already picks up src/*.cpp, but verify):\n- src/society/society.cpp\n- src/society/agent_handle.cpp\n- src/language/llm_provider.cpp\n- src/language/dialog_manager.cpp\n- src/learning/continuous_loop.cpp\nSince file(GLOB_RECURSE AI_LEARNING_SOURCES \"src/*.cpp\") already covers src/ recursively,\nthese should be auto-discovered. But EXCLUDE the src/server/ filter should still apply.\nJust verify the existing GLOB_RECURSE works correctly for the new directories.\n\nLazy initialization pattern:\n- In each register_* method, use a lambda that lazily creates the object on first request\n- Example: if (!society_) society_ = std::make_unique<Society>(SocietyConfig{});\n- llm_provider_: check env var DASHSCOPE_API_KEY first, if set use OpenAICompatibleProvider, else StubLLMProvider\n\nPattern to follow (from existing code):\n- Use crow macros with fully qualified ::crow namespace\n- JSON parsing: json::parse(req.body)\n- Error handling: try/catch with dto::make_error_response\n- Response: set Content-Type to application/json',
  { label: 'rest-integration', phase: 'REST Integration' }
)

log('REST integration: ' + (restIntegration ? 'done' : 'failed'))

// Phase 3: Web console update
phase('Web Console')

const webUpdate = await agent(
  'Active task: .trellis/tasks/06-02-ailearning-phase-7-9\n\n## Phase 9 Web Console Update\n\nUpdate the existing web console for Phase 9: Chat interface, Society panel, Runtime controls.\n\nREAD FIRST:\n- ai-learning-cpp/web/index.html — current HTML structure\n- ai-learning-cpp/web/app.js — current Vue 3 app logic\n- ai-learning-cpp/web/style.css — current dark theme styles\n\n### New UI Sections:\n\n#### 1. Chat Panel (right panel, after Q&A section)\nConversational chat interface:\n- Message display area (user right-aligned blue, assistant left-aligned dark)\n- Input field + send button\n- Shows learned knowledge tags under assistant messages\n- Scrollable, max-height 300px\n\n#### 2. Society Panel (left panel, after knowledge graph)\n- Agent list with status indicators\n- Create agent button (asks for port)\n- Remove agent button per agent\n- Social metrics display\n- Trigger observation button\n\n#### 3. Runtime Controls (top of right panel)\n- Start/Stop continuous learning buttons\n- Checkpoint button\n- Status line: uptime, iterations, retention, pending data\n\n### app.js additions:\nNew reactive state: chatMessages, chatInput, chatBusy, chatSessionId, societyAgents, societyMetrics, runtimeStatus\nNew API methods: doChat(), fetchChatHistory(), createAgent(), fetchSocietyMetrics(), startRuntime(), stopRuntime(), doCheckpoint(), fetchRuntimeStatus()\nAuto-refresh runtime status every 5s when running.\n\n### style.css additions:\n.chat-messages, .chat-msg, .msg-user, .msg-assistant, .msg-learned, .society-panel, .runtime-bar\n\nAll text in Chinese. Keep existing dark theme. Only ADD, do not remove existing sections.',
  { label: 'web-console', phase: 'Web Console' }
)

log('Web console: ' + (webUpdate ? 'done' : 'failed'))

// Phase 4: Build verify + chat test with real Tongyi API
phase('Verify')

const verify = await agent(
  "Active task: .trellis/tasks/06-02-ailearning-phase-7-9\n\n## Phase 9 Build Verification + Chat Test\n\nBuild and test Phase 9 implementation including real LLM chat.\n\nSteps:\n1. cd ai-learning-cpp\n2. mkdir -p build-server (if not exists)\n3. cd build-server\n4. cmake .. -G \"MinGW Makefiles\" -DCMAKE_BUILD_TYPE=Release\n5. cmake --build . --target ai_learning_server -j8\n6. If build fails: read errors, fix, rebuild\n7. If build succeeds, smoke test:\n   - Set env: export DASHSCOPE_API_KEY=sk-b68b1187aba542c3b2fec09cdc02634c\n   - PATH=\"/d/msys64/ucrt64/bin:$PATH\" DASHSCOPE_API_KEY=sk-b68b1187aba542c3b2fec09cdc02634c ./ai_learning_server.exe --port 8765 &\n   - sleep 3\n   - curl -s http://127.0.0.1:8765/api/health\n   - curl -s http://127.0.0.1:8765/api/runtime/status\n   - curl -s http://127.0.0.1:8765/society/agents\n   - Test chat: curl -s -X POST http://127.0.0.1:8765/api/chat -H \"Content-Type: application/json\" -d \"{\\\"message\\\":\\\"hello\\\",\\\"session_id\\\":\\\"test\\\"}\"\n   - Test knowledge teaching: curl -s -X POST http://127.0.0.1:8765/api/chat -H \"Content-Type: application/json\" -d \"{\\\"message\\\":\\\"photosynthesis is the process by which plants convert sunlight into energy\\\",\\\"session_id\\\":\\\"test\\\"}\"\n   - curl -s http://127.0.0.1:8765/api/stats\n   - kill server\n8. Also run existing tests: cd ../build-win && cmake .. -G \"MinGW Makefiles\" && cmake --build . --target ai_learning_tests -j8 && PATH=\"/d/msys64/ucrt64/bin:$PATH\" ./ai_learning_tests.exe [phase5] -s\n9. Report all results\n\nImportant:\n- Windows needs PATH=\"/d/msys64/ucrt64/bin:$PATH\" for DLLs\n- DASHSCOPE_API_KEY env var is critical for real LLM chat\n- Fix any compilation errors directly",
  { label: 'verify-build', phase: 'Verify' }
)

log('Verify: ' + (verify ? 'done' : 'failed'))

return {
  society: society ? 'completed' : 'failed',
  llmDialog: llmProvider ? 'completed' : 'failed',
  continuousLoop: continuousLoop ? 'completed' : 'failed',
  restIntegration: restIntegration ? 'completed' : 'failed',
  webConsole: webUpdate ? 'completed' : 'failed',
  verify: verify ? 'completed' : 'failed',
}
